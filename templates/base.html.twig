<!DOCTYPE html>
<html lang="es">
    <head>
        <meta charset="UTF-8">
        <meta name="robots" content="noindex" />
        
        <title>{% block title %}Comisiones{% endblock %}</title>
        <link rel="icon" href="{{ asset('resources/img/favicon.ico') }}" />
        {% block stylesheets %}
            <link href="{{ asset('resources/normalize/normalize.css')  }}" rel="stylesheet" />
            <link href="{{ asset('resources/bootstrap-5.1.1-dist/css/bootstrap.min.css')  }}" rel="stylesheet" />
            <link href="{{ asset('resources/bootstrap-icons-1.7.2/bootstrap-icons.css')  }}" rel="stylesheet" />
        {% endblock %}
   </head>
    <body class="bg-secondary">
        {% block body %}{% endblock %}

        {% block javascripts %}
            <script src="{{ asset('resources/jquery-3.6.0/jquery-3.6.0.min.js') }}" type="text/javascript"></script>
        <script src="{{ asset('resources/popper/popper.min.js')  }}" type="text/javascript"></script>
            <script src="{{ asset('resources/bootstrap-5.1.1-dist/js/bootstrap.min.js') }}" type="text/javascript"></script>
         {% endblock %}
    </body>
</html>
